{
  "name": "@openagents-org/agent-launcher",
  "version": "0.2.183",
  "description": "Local workspace connector daemon — joins a workspace and exposes local-computer capabilities (filesystem, browser automation, notifications, timers/routines, knowledge base) as MCP tools",
  "main": "src/index.js",
  "bin": {
    "agn": "bin/agent-connector.js",
    "openagents": "bin/agent-connector.js",
    "agent-connector": "bin/agent-connector.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "eslint src/"
  },
  "keywords": [
    "openagents",
    "workspace",
    "daemon",
    "mcp",
    "local-tools",
    "cli"
  ],
  "author": "OpenAgents",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/openagents-org/openagents",
    "directory": "packages/agent-connector"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "src/",
    "bin/",
    "icons/",
    "README.md"
  ]
}
